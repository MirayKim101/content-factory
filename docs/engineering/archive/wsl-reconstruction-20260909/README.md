# Historical WSL reconstruction — superseded

This archive records the alternative reconstruction at `6e5097d`, made before
the owner supplied the newer Mac repository. It is evidence, not the active
product specification, migration graph or runtime guide. Relative links and
process identifiers inside archived files describe that historical checkout.
The original code and migration bytes remain available through Git history and
the `recovery/linux-mvp-20260909` branch. See ADR-009 and the current handoff.
Do not apply these migrations to the Mac baseline or run either binary against
the other baseline's database. The eight pending WSL tests refer to the retired
implementation; they are not automatically the acceptance checklist for Mac.
